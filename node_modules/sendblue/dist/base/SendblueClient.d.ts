import { AxiosRequestConfig } from 'axios';
import { GroupMessageOptions, Groups } from '../rest/group-message';
import { MessageOptions, Messages } from '../rest/message';
import { RequestClient, RequestClientOptions, RequestClientRequestOptions } from './RequestClient';
export interface SendblueClientOptions {
    axiosClientOptions?: RequestClientOptions;
    logLevel?: 'debug' | 'info' | 'warn' | 'error';
}
export declare class SendblueClient {
    axiosClient: RequestClient;
    groups: Groups;
    messages: Messages;
    logLevel: 'debug' | 'info' | 'warn' | 'error';
    constructor(apiKey: string, apiSecret: string, options?: SendblueClientOptions);
    request(options: AxiosRequestConfig & RequestClientRequestOptions): Promise<any>;
    sendGroupMessage(options: GroupMessageOptions): Promise<import("./Message").MessageImpl>;
    sendMessage(options: MessageOptions): Promise<import("./Message").MessageImpl>;
}
